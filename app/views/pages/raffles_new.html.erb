<div class="dir">
    <div class="raf">
        <h2 class="title_padrao">Novas</h2>
        <div class="row">
          <% @raffles_new.each do |raffle| %>
            <%= render 'pages/raffles', :raffle => raffle %>
          <% end %>
        </div>
    </div>
</div>
