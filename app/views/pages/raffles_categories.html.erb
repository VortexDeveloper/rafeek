<div class="dir">
    <div class="raf">
        <h2 class="title_padrao"><%= @page_title %></h2>
        <div class="row">
          <% @raffles_categories.each do |raffle| %>
            <%= render 'pages/raffles', :raffle => raffle %>
          <% end %>
        </div>
    </div>
</div>
