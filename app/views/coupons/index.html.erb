<p id="notice"><%= notice %></p>

<section class="content-header">
  <h1>
    Cupons de desconto
  </h1>
</section>
<section class="content">
  <%= link_to 'Novo cupom', new_coupon_path, class: 'btn btn-lg btn-success pull-right m-bottom'  %>
  <div class="row">
    <div class="col-xs-12">
      <div class="box inner-content">
          <table id="example2" class="table table-bordered table-hover">
            <thead>
              <tr>
                <th>Logo</th>
                <th>Nome da Empresa</th>
                <th>Código</th>
                <th>Validade</th>
                <th>Desconto em %</th>
                <th>Ações</th>
              </tr>
            </thead>

            <tbody>
              <% @coupons.each do |coupon| %>
                <tr>
                  <td><%= coupon.logo %></td>
                  <td><%= coupon.name %></td>
                  <td><%= coupon.code %></td>
                  <td><%= coupon.validity %></td>
                  <td><%= coupon.discount %></td>
                  <td>
                    <%= link_to '', coupon, class: 'btn btn-info fa fa-location-arrow' %>
                    <%= link_to '', edit_coupon_path(coupon), class: 'btn btn-primary fa fa-edit' %>
                    <%= link_to '', coupon, method: :delete, data: { confirm: 'Deseja mesmo deletar esse cupom?' }, class: 'btn btn-danger  fa  fa-trash-o' %>
                  </td>
                </tr>
              <% end %>
            </tbody>
            <tfoot>
            </tfoot>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>
