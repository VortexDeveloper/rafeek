<div class="content">
    <div class="dir">
        <div class="perfil">
            <%= render partial: 'user/user_nav' %>
            <div class="perfil_desc">
                <div class="caption rifa">
                    <h3>Editar Conta</h3>
                    <div class="row">
                        <div class="form-group">
                            <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
                              <%= devise_error_messages! %>

                              <div class="field">
                                <%= f.label :email %><br />
                                <%= f.email_field :email, class: 'form-control', autofocus: true, required: true %>
                              </div>

                              <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                                <p>
                                  <%= t('.currently_waiting_confirmation_for_email', email: resource.unconfirmed_email) %>
                                </p>
                              <% end %>

                              <div class="field">
                                <%= f.label :cpf, 'CPF' %><br />
                                <%= f.text_field :cpf, class: 'form-control', required: true %>
                              </div>

                              <div class="field">
                                <%= f.label :password %> <i>(<%= t('.leave_blank_if_you_don_t_want_to_change_it') %>)</i><br />
                                <%= f.password_field :password, class: 'form-control', autocomplete: "off", required: true %>
                                <% if @minimum_password_length %>
                                  <br />
                                  <em><%= @minimum_password_length %> caracteres no mínimo</em>
                                <% end %>
                              </div>

                              <div class="field">
                                <%= f.label :password_confirmation %><br />
                                <%= f.password_field :password_confirmation, class: 'form-control', autocomplete: "off", required: true %>
                              </div>

                              <div class="field">
                                <%= f.label :current_password %> <i>(<%= t('.we_need_your_current_password_to_confirm_your_changes') %>)</i><br />
                                <%= f.password_field :current_password, class: 'form-control', autocomplete: "off", required: true %>
                              </div>

                              <div class="actions-buttons">
                                <%= f.submit t('.update') %>
                              </div>
                              <div class="actions-buttons cancel-button">
                                  <%= link_to "Voltar", :back, class: 'back-button actions-buttons' %>
                              </div>
                            <% end %>
                            <div class="cancel-button actions-buttons">
                              <%= button_to t('.cancel_my_account'), registration_path(resource_name), data: { confirm: t('.are_you_sure') }, method: :delete %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
