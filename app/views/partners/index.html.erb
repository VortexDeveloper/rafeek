<p id="notice"><%= notice %></p>

<section class="content-header">
  <h1>
    Parceiros
  </h1>
</section>
<section class="content">
  <%= link_to 'Novo parceiro', new_partner_path, class: 'btn btn-lg btn-success pull-right m-bottom' %>
  <div class="row">
    <div class="col-xs-12">
      <div class="box inner-content">
          <table id="example2" class="table table-bordered table-hover">
            <thead>
              <tr>
                <th>Nome</th>
                <th>Logo</th>
                <th>Url</th>
                <th>Ações</th>
              </tr>
            </thead>

            <tbody>
              <% @partners.each do |partner| %>
                <tr>
                  <td><%= partner.name %></td>
                  <td><%= partner.logo %></td>
                  <td><%= partner.url %></td>
                  <td>
                    <%= link_to '', partner, class: 'btn btn-info fa fa-location-arrow' %>
                    <%= link_to '', edit_partner_path(partner), class: 'btn btn-primary fa fa-edit' %>
                    <%= link_to '', partner, method: :delete, data: { confirm: 'Deseja mesmo deletar esse parceiro?' }, class: 'btn btn-danger fa  fa-trash-o' %>
                  </td>
                </tr>
              <% end %>
            </tbody>
            <tfoot>
            </tfoot>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>
<br>
